"""Bundled frontend assets.

The ``dist/`` subdirectory is populated by ``npm run build`` inside ``web/``
and is listed in ``.gitignore``.  When the directory is absent, ``skillscope
serve`` runs the API without static file hosting.
"""
